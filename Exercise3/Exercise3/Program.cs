using System;
using Exercise3.Domain;

namespace Exercise3
{
    enum DismissalReason
    {
        FamilyReasons,
        ProfessionalGrowthLack,
        LowSalary,
        BadTeamMicroclimate,
        LackManagementUnderstanding,
        Other
    }
    class Program
    {
        static void Main(string[] args)
        {
            /*Candidate t = new Candidate();
            Console.WriteLine(t);*/
            /*foreach (DismissalReason foo in Enum.GetValues(typeof(DismissalReason))) ;*/




            /*Person id = new Person();
            Console.WriteLine(id.CreateId());
            
            Person fName = new Person();
            Console.WriteLine(fName.CreateFirstName());
            
            Person lName = new Person();
            Console.WriteLine(lName.CreateLastName());
            
            Person jobSalary = new Person();
            Console.WriteLine(jobSalary.CreateJobSalary());
            
            Person jobDescription = new Person();
            Console.WriteLine(jobDescription.CreateJobDescription());
            
            Person jobTitle = new Person();
            Console.WriteLine(jobTitle.CreateJobTitle());*/
        }
    }
}
